数据库表信息如下：
## 数据库表结构说明

### 表名：projects（项目表）

**字段说明：**
- `id`：主键，整数类型，自动递增，唯一标识每个项目
- `project_name`：项目名称，字符串类型(最大100字符)，不能为空，具有唯一约束
- `budget`：项目预算，精确数值类型(总位数15，小数位2)，表示项目的预算金额
- `created_at`：创建时间，时间戳类型，记录项目创建时间（默认当前时间）
- `updated_at`：更新时间，时间戳类型，记录项目最后更新时间（自动更新）